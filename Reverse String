class Solution {
    public void reverseString(char[] s) {
        int n=0,m=s.length-1;
        while(n<m){
            char temp = s[n];
            s[n]=s[m];
            s[m]=temp;
            n++;
            m--;
        }
    }
}
